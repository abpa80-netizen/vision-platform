"use client";
import {useEffect,useState}from"react";import{createClient}from"../../lib/supabase";import Link from"next/link";
export default function SuperAdmin(){const[stores,setStores]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=createClient();const{data:u}=await s.auth.getUser();if(!u.user){location.href="/login";return}const{data:p}=await s.from("profiles").select("role").eq("id",u.user.id).single();if(p?.role!=="super_admin"){location.href="/";return}const{data}=await s.from("stores").select("id,name,slug,status,created_at").order("created_at",{ascending:false});setStores(data||[]);setLoading(false)})()},[]);return <main className="container"><nav className="nav" style={{margin:"-24px -24px 24px"}}><div className="brand">VISION <span>SHOP</span> · SUPER ADMIN</div><Link className="btn dark" href="/">Accueil</Link></nav><h1>Console multi-boutiques</h1><div className="grid"><div className="card"><h3>Boutiques</h3><div className="price">{stores.length}</div></div><div className="card"><h3>Sécurité</h3><div className="ok">RLS activé</div></div><div className="card"><h3>Plateforme</h3><div className="ok">Opérationnelle</div></div></div><h2>Boutiques</h2>{loading?<p>Chargement...</p>:<div className="card"><table className="table"><thead><tr><th>Nom</th><th>Slug</th><th>Statut</th><th></th></tr></thead><tbody>{stores.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.slug}</td><td>{x.status}</td><td><Link className="btn dark" href={"/store/"+x.slug+"/admin"}>Ouvrir</Link></td></tr>)}</tbody></table></div>}</main>}
"use client";
import{useEffect,useState}from"react";import{createClient}from"../../../../lib/supabase";import Link from"next/link";
export default function Admin({params}:{params:Promise<{slug:string}>}){
 const[slug,setSlug]=useState(""),[store,setStore]=useState<any>(),[products,setProducts]=useState<any[]>([]),[name,setName]=useState(""),[slogan,setSlogan]=useState(""),[description,setDescription]=useState(""),[newName,setNewName]=useState(""),[newPrice,setNewPrice]=useState(""),[newOldPrice,setNewOldPrice]=useState(""),[newDesc,setNewDesc]=useState(""),[newStock,setNewStock]=useState(""),[newSku,setNewSku]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[msg,setMsg]=useState("");
 async function load(sl:string){const s=createClient();const{data:u}=await s.auth.getUser();if(!u.user){location.href="/login";return}const{data:st}=await s.from("stores").select("*").eq("slug",sl).maybeSingle();if(!st){setLoading(false);return}const{data:m}=await s.from("store_users").select("role").eq("store_id",st.id).eq("user_id",u.user.id).maybeSingle();if(!m){location.href="/store/"+sl;return}const{data:p}=await s.from("products").select("id,name,description,price,old_price,stock,sku,status").eq("store_id",st.id).order("created_at",{ascending:false});setStore(st);setName(st.name);setSlogan(st.slogan||"");setDescription(st.description||"");setProducts(p||[]);setLoading(false)}
 useEffect(()=>{params.then(({slug})=>{setSlug(slug);load(slug)})},[params]);
 async function saveStore(e:React.FormEvent){e.preventDefault();setSaving(true);setMsg("");const{error}=await createClient().from("stores").update({name:name.trim(),slogan:slogan.trim(),description:description.trim()}).eq("id",store.id);setMsg(error?error.message:"Boutique mise à jour.");setSaving(false);if(!error)setStore({...store,name,slogan,description})}
 async function addProduct(e:React.FormEvent){e.preventDefault();setMsg("");const price=Number(newPrice);if(!newName.trim()||!Number.isFinite(price)){setMsg("Nom et prix valides requis.");return}const payload:any={store_id:store.id,name:newName.trim(),description:newDesc.trim(),price,status:"published",stock:Number(newStock)||0,sku:newSku.trim()||null};if(newOldPrice)payload.old_price=Number(newOldPrice);const{data,error}=await createClient().from("products").insert(payload).select("id,name,description,price,old_price,stock,sku,status").single();if(error){setMsg(error.message);return}setProducts([data,...products]);setNewName("");setNewPrice("");setNewOldPrice("");setNewDesc("");setNewStock("");setNewSku("");setMsg("Produit ajouté.");
}
 async function delProduct(id:string){if(!confirm("Supprimer ce produit ?"))return;const{error}=await createClient().from("products").delete().eq("id",id).eq("store_id",store.id);if(error){setMsg(error.message);return}setProducts(products.filter(p=>p.id!==id));}
 async function toggleProduct(p:any){const{data,error}=await createClient().from("products").update({status:p.status==="published"?"draft":"published"}).eq("id",p.id).select("id,name,description,price,old_price,stock,sku,status").single();if(error){setMsg(error.message);return}setProducts(products.map(x=>x.id===p.id?data:x));}
 const catalogUrl=typeof window!=="undefined"?window.location.origin+"/store/"+slug:"/store/"+slug;
 const wa="https://wa.me/212655996172?text="+encodeURIComponent("Bonjour, voici notre catalogue VISION SHOP : "+catalogUrl);
 if(loading)return <main className="admin-shell"><div className="admin-loading">Chargement de votre espace…</div></main>;if(!store)return <main className="container"><h1>Boutique introuvable</h1></main>;
 return <main className="admin-shell"><header className="admin-top"><div><div className="admin-brand">VISION <span>SHOP</span></div><div className="admin-store">{store.name}<small>ESPACE ADMINISTRATEUR</small></div></div><div className="admin-actions"><Link className="btn secondary" href={"/store/"+slug}>Voir la boutique</Link><button className="btn green" onClick={()=>navigator.clipboard?.writeText(catalogUrl)}>Copier le lien catalogue</button></div></header><div className="admin-layout"><aside className="admin-sidebar"><Link href={"#dashboard"}>Vue d'ensemble</Link><Link href={"#products"}>Produits</Link><Link href={"#store"}>Boutique</Link><Link href={"#catalogue"}>Lien catalogue</Link><a href={wa} target="_blank">Partager WhatsApp</a></aside><section className="admin-main"><div className="admin-hero"><div><span className="eyebrow">TABLEAU DE BORD</span><h1>Gérez votre boutique simplement.</h1><p>Produits, identité et catalogue client depuis un seul espace.</p></div><div className="status-pill">● {store.status}</div></div>{msg&&<div className="notice">{msg}</div>}<div className="stats"><div><span>Produits</span><strong>{products.length}</strong></div><div><span>Publiés</span><strong>{products.filter(p=>p.status==="published").length}</strong></div><div><span>Catalogue</span><strong>Actif</strong></div></div><section id="products" className="panel"><div className="panel-head"><div><span className="eyebrow">CATALOGUE</span><h2>Ajouter un produit</h2></div></div><form className="product-form" onSubmit={addProduct}><input className="input" placeholder="Nom du produit" value={newName} onChange={e=>setNewName(e.target.value)} required/><input className="input" type="number" min="0" step="0.01" placeholder="Prix" value={newPrice} onChange={e=>setNewPrice(e.target.value)} required/><input className="input" type="number" min="0" step="0.01" placeholder="Ancien prix (optionnel)" value={newOldPrice} onChange={e=>setNewOldPrice(e.target.value)}/><input className="input" type="number" min="0" placeholder="Stock" value={newStock} onChange={e=>setNewStock(e.target.value)}/><input className="input" placeholder="SKU (optionnel)" value={newSku} onChange={e=>setNewSku(e.target.value)}/><textarea className="input" placeholder="Description du produit" value={newDesc} onChange={e=>setNewDesc(e.target.value)}/><button className="btn green">Ajouter au catalogue</button></form></section><section className="panel"><div className="panel-head"><div><span className="eyebrow">PRODUITS</span><h2>Votre catalogue</h2></div><span className="muted">{products.length} produit(s)</span></div><div className="product-list">{products.map(p=><div className="product-row" key={p.id}><div><strong>{p.name}</strong><p>{p.description||"Sans description"} · Stock: {p.stock??0}</p></div><div className="product-price">{p.price} {store.currency}</div><button className="btn secondary" onClick={()=>toggleProduct(p)}>{p.status==="published"?"Désactiver":"Activer"}</button><button className="btn danger-btn" onClick={()=>delProduct(p.id)}>Supprimer</button></div>)}{products.length===0&&<div className="empty-state"><strong>Votre catalogue est vide</strong><p>Ajoutez votre premier produit ci-dessus pour le rendre visible aux clients.</p></div>}</div></section><section id="store" className="panel"><div className="panel-head"><div><span className="eyebrow">IDENTITÉ</span><h2>Personnaliser la boutique</h2></div></div><form onSubmit={saveStore}><label>Nom de la boutique</label><input className="input" value={name} onChange={e=>setName(e.target.value)} required/><label>Slogan</label><input className="input" value={slogan} onChange={e=>setSlogan(e.target.value)}/><label>Description</label><textarea className="input" value={description} onChange={e=>setDescription(e.target.value)}/><button className="btn">Enregistrer les modifications</button></form></section><section id="catalogue" className="panel"><span className="eyebrow">PARTAGE</span><h2>Votre lien catalogue</h2><p className="muted">{catalogUrl}</p><div className="admin-actions"><button className="btn" onClick={()=>navigator.clipboard?.writeText(catalogUrl)}>Copier le lien</button><a className="btn green" href={wa} target="_blank">Partager sur WhatsApp</a><Link className="btn secondary" href={"/store/"+slug}>Ouvrir le catalogue</Link></div></section></section></div></main>
}